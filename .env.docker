# ============================================================================
# 🐳 DOCKER DEPLOYMENT ENVIRONMENT VARIABLES
# ============================================================================
# Copy this to .env when deploying to Docker

# Production flag
ENVIRONMENT=production

# Database (persistable via volume mounting)
DATABASE_URL=sqlite:///./bharat.db

# JWT Security - CHANGE THIS IN PRODUCTION!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secure-secret-key-change-this-in-production

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here

# Groq AI
GROQ_API_KEY=your-groq-api-key-here

# CORS - Allow frontend container
CORS_ALLOW_ORIGIN=http://localhost:3000,http://frontend:3000,http://127.0.0.1:3000
